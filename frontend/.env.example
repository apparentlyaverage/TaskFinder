# ── Backend URL ────────────────────────────────────────────────────────────────
# PRODUCTION (set in Vercel → Project → Settings → Environment Variables):
#   VITE_API_URL=https://api.relivr.co.za
# Local dev: leave blank — Vite proxies relative paths to localhost:3001 automatically.
VITE_API_URL=
