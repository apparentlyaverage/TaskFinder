# ── Database (Neon) ────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://taskfinder_owner:PASSWORD@ep-xxx.eu-central-1.aws.neon.tech/taskfinder?sslmode=require

# ── JWT + Session ──────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate-a-32-byte-random-hex-string
JWT_EXPIRES_IN=7d
SESSION_SECRET=generate-another-32-byte-random-hex-string

# ── Google OAuth ───────────────────────────────────────────────────────────────
# Create credentials at https://console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxx
# Local dev (Vite proxies port 3000 → 3001):
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback
# Production Railway — also add this URI in Google Console:
# GOOGLE_CALLBACK_URL=https://YOUR-APP.up.railway.app/auth/google/callback

# ── URLs ───────────────────────────────────────────────────────────────────────
# Must match your Vercel deployment URL (used for CORS + OAuth redirects)
FRONTEND_URL=http://localhost:3000

# ── Email (Resend) ─────────────────────────────────────────────────────────────
# All transactional mail (verify email, password reset, waitlist) is sent via Resend.
# Leave RESEND_API_KEY blank in dev — emails are logged to console instead of sent.
# Verify your domain at https://resend.com before setting this in production.
EMAIL_FROM=ReLivR <noreply@relivr.co.za>
RESEND_API_KEY=

# ── Observability ──────────────────────────────────────────────────────────────
# Sentry error tracking DSN. Leave blank — errors go to structured logs until set.
# Create a project at https://sentry.io and paste the DSN here.
SENTRY_DSN=

# ── Server ─────────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
